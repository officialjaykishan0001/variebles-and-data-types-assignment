#include<iostream>
using namespace std;
//Q : take the length and breath of the rectangle and return the area of the rectangle.
int main()
{
    int l,b;

    cout<<"enter the value of the length : ";
    cin>>l;
    cout<<"enter the value of the breath : ";
    cin>>b;

    cout<<"the area of the rectangle is : "<<(l*b)<<endl;

    return 0;
}